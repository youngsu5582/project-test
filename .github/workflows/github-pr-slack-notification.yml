name: send slack mention message to requested reviewer
on:
  pull_request:
    branches:
      - main
      - develop
    types:
      - review_requested

jobs:
  specific_review_requested:
    runs-on: ubuntu-latest
    steps:
      - name: Extract REVIEWERS
        run: |
          slack_ids_json='{
            "i894": "U07B4GGQ5EX",
            "youngsu5582": "U07BH9G42AV",
          }'
          
          mentions=""

          for reviewer in $(echo '${{ toJSON(github.event.pull_request.requested_reviewers) }}' | jq -c '.[]'); do
            # Extract the reviewer's login name
            login=$(echo "$reviewer" | jq -r '.login')

            slack_id=$(echo "$slack_ids_json" | jq -r --arg login "$login" '.[$login]')

            if [ -n "$slack_id" ]; then
              mentions+=" <@$slack_id>"
            fi
          done
          echo "REVIEWER_SLACK_IDS=$mentions" >> $GITHUB_ENV

      - name: Extract ASSIGNEES
        env:
          PULL_REQUEST_CONTEXT: ${{ toJson(github.event.pull_request) }}
        run: |
          slack_ids_json='{
            "i894": "U07B4GGQ5EX",
            "youngsu5582": "U07BH9G42AV",
          }'
          
          mentions=""

          for reviewer in $(echo '${{ toJSON(github.event.pull_request.assignees) }}' | jq -c '.[]'); do
            # Extract the reviewer's login name
            login=$(echo "$reviewer" | jq -r '.login')

            slack_id=$(echo "$slack_ids_json" | jq -r --arg login "$login" '.[$login]')

            if [ -n "$slack_id" ]; then
              mentions+=" <@$slack_id>"
            fi
          done
          echo "ASSIGNEES_SLACK_IDS=$mentions" >> $GITHUB_ENV          


      - name: pr reviewer 되면 slack 알림 보냄
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: ${{ secrets.REVIEW_MENTION_CHANNEL_ID }}
          payload: |
            {
              "text": "PR review request",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "<@${{ env.ASSIGNEES_SLACK_IDS }}>가 \n ${{ env.REVIEWER_SLACK_IDS }} 들에게 리뷰 요청을 했어요🚀🚀"
                  }
                }
              ],
              "attachments": [
                {
                  "fallback": "[CoReA] New Pull Request",
                  "color": "#00E000",
                  "author_name": "${{ github.event.sender.login }}",
                  "title": "${{ github.event.pull_request.title }}",
                  "title_link": "${{ github.event.pull_request.html_url }}",
                  "text": "${{ github.event.pull_request.body }}",
                  "short": true
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_WEBHOOK_URL }}
